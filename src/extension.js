/**
 * openFuyao 扩展组件配置文件
 * 定义扩展组件的唯一名称，用于在 openFuyao 前端管理面中创建挂载点
 */
const config = {
  menu: {
    // 扩展组件名称（必须唯一）
    // openFuyao 将创建 id 为 "rainbond_root" 的挂载点
    pluginName: 'rainbond',
  },
};

export default config;
